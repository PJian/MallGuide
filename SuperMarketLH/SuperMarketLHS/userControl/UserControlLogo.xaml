<UserControl x:Class="SuperMarketLHS.userControl.UserControlLogo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             xmlns:converter="clr-namespace:ResourceManagementService.converter;assembly=ResourceManagementService"
             d:DesignHeight="250" d:DesignWidth="200" Loaded="UserControl_Loaded_1" >
    <UserControl.Resources>
        <converter:ImgConverter x:Key="imgConverter"></converter:ImgConverter>
    </UserControl.Resources>
    <Grid x:Name="grid_content">
        <Grid.RowDefinitions>
            <RowDefinition Height="200*"></RowDefinition>
            <RowDefinition Height="50*"></RowDefinition>
        </Grid.RowDefinitions>
        <Border Grid.Column="0" Grid.Row="0" BorderBrush="White" BorderThickness="2">
            <Image Source="{Binding ImgPath,Converter={StaticResource imgConverter}}" Margin="2"></Image>
        </Border>
        
        <Button x:Name="btn_addImg" Grid.Column="0" Grid.Row="1" Style="{DynamicResource navBtnCommonStyle}" Content="选择LOGO"  Background="Transparent" Width="70" Height="30" Click="btn_addImg_Click" ></Button>
    </Grid>
</UserControl>
